# v0.17.1 submission material

**Status:** completed, 2026-07-17.  
**Scope:** Devpost draft, timed video narrative, and a judge quick-start path.

All public copy must distinguish verified facts from planned work. The only
quality result cited is the held-out NL→SQL comparison: pass@1
`0.5833 → 0.7833` at selected step 350. Local vLLM serving is verified; the
public RunPod gateway timed out and must not be presented as a live endpoint.

The package must be reproducible from tracked demo artifacts with no model
weights, credentials, paid-provider request, or raw traffic data. It is not a
claim of a general benchmark, automated multi-node recovery, or production
availability.

Validation is editorial and technical: every number traces to
`data/demo-artifacts/`, each command is safe in artifact mode, and the
ten-minute path works without a GPU or cloud account.

The completed package is `devpost-draft.md`, `video-script.md`, and the
repository-root `JUDGES.md` guide.
