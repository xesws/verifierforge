# v0.28.2 — P-2 live closeout reservation

Current reviewer documents must continue to state that P-2 is incomplete. The
old billing-row blocker was corrected and orphan reaping passed, but the
required 100-step S3 collection did not pass. Deletion is now correctly proven
by DELETE plus target absence and raw prefix zero; billing reconciles
asynchronously.

The milestone claim must include the approval, pushed revision, instance
identity, estimated spend, S3 object hashes and cleanup evidence. It must not
claim an actual zero-dollar cost while billing is pending or imply that the
default-off product button launches a GPU.

The live result is a recorded non-pass, not a milestone claim. Gold and orphan
reaping succeeded, but four clean 0.5B attempts each reached 49 metrics and
failed closed at the step-50 serving publication gate. Every pod was deleted
and final raw prefix count is zero. The last root cause was a fixed vLLM memory
fraction exceeding volatile free memory beside the paused trainer. No
step-100 collection exists and `provisioner-p2-live` was not created.
