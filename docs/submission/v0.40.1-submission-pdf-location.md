# v0.40.1 — Technical Deep Dive attachment location

The canonical judge attachment lives at:

`docs/submission/verifierforge-technical-deep-dive.pdf`

Its adjacent `.sha256` file records the same byte identity as v0.40.0. The
exporter writes directly to this directory so future refreshes cannot recreate
the mistaken `output/pdf/` location.
