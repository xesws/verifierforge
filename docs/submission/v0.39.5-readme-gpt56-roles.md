# v0.39.5 — GPT-5.6 README placement

The GitHub reviewer narrative now explains GPT-5.6 immediately before the
Codex collaboration log. The copy identifies Soul Ultra as the collaborative
development partner, Luna as the model behind the strict-schema Agent Layer,
and GPT-5.6 as the model used to help generate C-code verifier candidates.

The section explicitly separates those roles from the trained Qwen specialist
and retains the human-review and validation boundary around generated
verifiers.
