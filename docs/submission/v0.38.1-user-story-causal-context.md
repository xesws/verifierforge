# v0.38.1 — submission causal-context revision

**Status:** complete; canonical and video copy carry the causal chain

## Intent

Revise the 25-second opening so a reviewer understands the real sequence:
employees repeatedly ask an internal assistant for workforce/project analytics;
the legacy implementation routes 95,000 monthly requests to a paid external
general model API; token-priced use creates the $5,500 monthly baseline; the
repetition and executable SQL verifier make a specialist model defensible.

## Provider wording

Use “external large-model API” in the voiceover. OpenRouter is a valid example
of the repository's configurable OpenAI-compatible upstream, but the product
catalog does not bind the $5,500 fact to OpenRouter or any single provider.

## Acceptance

Both languages must preserve the causal chain, duration limits, exact catalog
numbers, schema-backed objects, and provider-neutral truth boundary.

Acceptance passed with 110 Chinese characters, 61 English words, three
sentences per language, an unchanged 0:00–3:00 timeline, and full pytest at
`492 passed, 1 skipped, 1 warning` (the pre-existing aiosqlite shutdown race).
