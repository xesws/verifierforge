# v0.28.5 — P-2 live-pass narrative

Reviewer-facing documents may claim P-2 completion only after the tracked
sanitized evidence, final run-sheet checks and `provisioner-p2-live` tag are
committed together. The claim is narrow: an approved 0.5B, 100-step RunPod job
stored candidate and published checkpoints through S3, passed a post-training
same-GPU vLLM models/completion gate, was SHA-collected, and was deleted with
target-absent plus raw-prefix-zero evidence.

Do not claim that the product's Approve button directly launches a GPU; the
live proof used the separately authorized P-2 executor. Do not report pending
billing as an actual cost. The run's provider estimate was `$0.177846`, the
conservative five-attempt wave estimate was `$2.363580`, and +1h/+6h billing
reconciliation remains asynchronous.

README, JUDGES, Devpost and video guidance must retain the default-off Agent,
ephemeral public endpoint and single NL→SQL vertical limitations while removing
only the obsolete statement that P-2 stopped before orphan/training execution.

