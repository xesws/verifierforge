# v0.30.0 — Full reviewer sandbox handoff

Judges get two paths: a dependency-free evidence fallback and an owner-hosted
full path. The full path demonstrates Supabase-backed Discover, deterministic
Agent analysis, approval, mock Start Forge lifecycle, real tuned proxy routing
and Guardian points behind invitation auth. It makes no paid LLM or GPU call.

The public URL is ephemeral and must be paired with the separately shared Basic
Auth invitation. Named-tunnel setup remains an owner action.
