# v0.39.4 — Agent figure semantic layout

Figure 2 keeps its evidence, title, and metrics unchanged. The correction is
purely spatial and semantic:

- the Gate C Evaluator outline encloses `submit_decision` and its score badge;
- the evaluator label is centered over the evaluated system;
- the human side contains only approval and the separately authorized Start;
- a visible flow connects the validated terminal decision to human review,
  then human approval to Start Forge.

The generated SVG remains derived from
`runs/forge-agent/gate-c-v0223-round2.json` with its source SHA embedded.

The generator now enforces this semantic layout with a regression test that
checks the evaluator boundary contains both the strict decision exit and the
Gate C score badge.
