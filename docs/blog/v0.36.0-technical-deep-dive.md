# v0.36.0 blog delivery record

**Status:** planned

`technical-deep-dive.md` will be the only prose source for the technical blog.
It must distinguish measured facts, provider estimates, historical test-suite
snapshots, and limitations. Every quantitative statement will cite a tracked
run-sheet, manifest, metrics, or evidence path.

Six SVGs are mandatory: the M3/M4 falsification reference, Forge Agent system,
GRPO loop, NL2SQL verifier tiers, held-out checkpoint selection, and the full
six-step system loop. The two data plots must read tracked JSON/JSONL; conceptual
diagrams may not invent performance curves. Each figure needs an independent
caption, accessible title/description, and deterministic source metadata.

Primary research references are limited to DeepSeekMath/GRPO
(`arXiv:2402.03300`), one-shot RLVR (`arXiv:2504.20571`), and DeepSeek-R1
(`arXiv:2501.12948`). The article uses “builds on” or “inspired by” and claims
an engineering system, not an original RL theory.

