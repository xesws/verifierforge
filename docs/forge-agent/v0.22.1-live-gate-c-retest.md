# v0.22.1 — Live Gate C retest

This patch closes the evidence gap created by the unverified
`gpt-5.6-luna-xhigh` model name. The formal model is selected only after an
authenticated `/v1/models` query and passed through `VF_AGENT_EVAL_MODEL`.

The frozen 12-scenario dataset, dependency-chain scorer, and thresholds remain
unchanged. A pass may create `agent-gate-c-pass`; it must not turn on
`VF_AGENT_ENABLED` or `VF_AGENT_GATE_C_PASSED` automatically.

Discovery evidence from the official endpoint is `gpt-5.6-luna`,
`gpt-5.6-sol`, and `gpt-5.6-terra`. Gate C selects the first exact ID; the other
two remain policy-forbidden.

The live result did not pass: decision accuracy `0.0`, chain success `0.0`,
illegal actions `0`, and config legality `0.0`. Each of the 12 request attempts
ended as `LLMRequestError` before a tool call; no retry was issued. The admission
tag and feature flag remain unchanged.
