# v0.22.2 — Gate C second retest

This wave preserves the frozen evaluator and decision thresholds. It repairs
transport evidence, probes Chat Completions versus Responses function calling,
and adds a fail-closed flight check before the 12 live scenarios.

The feature flag remains off regardless of the result. Passing Gate C creates
only the `agent-gate-c-pass` evidence tag; deployment remains an owner action.

Both Responses preflight probes passed, so the batch was admitted. The final
four metrics were decision accuracy `0.6666666666666666`, chain success
`0.8333333333333334`, illegal actions `0`, and config legality `0.5`. The gate
did not pass; no threshold was modified and no tag was created.
