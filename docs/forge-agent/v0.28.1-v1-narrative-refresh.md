# v0.28.1 Forge Agent narrative

Reviewer docs will describe the real flow and its boundaries: Gate C passed
with no illegal actions and valid configs; `VF_AGENT_ENABLED` remains false by
default; Analyze is cacheable; Approve writes an approval only. Execution is
not presented as a button-side effect.
