# v0.4.11 — Backend API: D2 Acceptance Evidence

**Status:** Complete

## Scope

Record local API validation for the real synced run and the existing
Copilot-route unit validation. `/jobs` and `/jobs/{id}/metrics` remain
file-backed and contract-compatible.

## Required Evidence

- `/jobs` infers `d2demo` as done after the final manifest arrives.
- `/jobs/d2demo/metrics` returns the 100-step Metrics curve from synced JSONL.
- Copilot route tests use injected fake provider/sandbox clients; a live
  OpenRouter call is not implied unless separately evidenced.

## Non-goals

No provider key, browser interaction, UI implementation, or API-contract
expansion is included.

## Observed Evidence — 2026-07-15

After `vf watch`, the local API returned `d2demo` with status `done` from
`GET /jobs` and served its 100-point Metrics curve from
`GET /jobs/d2demo/metrics`. Focused `test_openrouter_client.py`,
`test_copilot_api.py`, `test_nl2sql_v1_data.py`, and
`test_nl2sql_verifier.py` passed 28 tests with injected clients/sandbox
dependencies; no live provider request was made.
