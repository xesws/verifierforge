# v0.5.0 — Backend API Notes (Contracts + Mock)

**Status:** Complete

Adds additive shared models for clusters, routing, live pass-rate series, and
optional report arena/savings fields. Real API gains only a minimal
`GET /jobs/{job_id}` that builds a `Job` from existing `runs/` inference
(placeholders for template/model/control). Full list/report parity with mock
is deferred.

Validation: `pytest -q` including contract and API job-detail coverage.
