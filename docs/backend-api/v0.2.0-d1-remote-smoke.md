# v0.2.0 — Backend API: D1 Remote Smoke

## Planned Change

Verify that the local API reads the `runs/<job>/` data synchronized by `vf watch` without a database or API contract change.

## Acceptance Evidence

Record `GET /jobs` and `GET /jobs/<id>/metrics` JSON validation after the remote fake job. Confirm the job status and metrics arrays reflect the synchronized files and that no contract change is required.

## Evidence Record

With the local API served on `127.0.0.1:8010` (port 8000 was already in use by another local process), `GET /jobs` returned `[{"job_id":"demo1","status":"done"}]`. `GET /jobs/demo1/metrics` returned valid `Metrics` JSON with 150 values for each of `steps`, `reward_mean`, `pass_at_1`, and `entropy`; the final step was 150. No API or shared-contract change was needed.
