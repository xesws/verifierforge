# v0.22.1 — Authenticated model discovery

Production model IDs must be copied from the authenticated provider's
`GET /v1/models` response. Documentation, memory, aliases, and guessed effort
suffixes are not accepted as availability evidence.

For this Gate C retest, the OpenAI key remains local in `.env`; only matching
model IDs and aggregate evaluation evidence may enter version documents. API
keys and raw headers must never be logged or committed.

The authenticated response returned exactly three matching IDs:
`gpt-5.6-luna`, `gpt-5.6-sol`, and `gpt-5.6-terra`. The selected configuration
uses exact ID `gpt-5.6-luna`; the user-suggested provider-prefixed spelling was
not returned and was not used.
