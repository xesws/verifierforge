# v0.28.1 delivery narrative

Reviewer docs will replace the stale “public endpoint unavailable” statement
with the recorded Cloudflare quick-tunnel proof (120 default / 80 tuned,
Guardian 0.85, then canary zero). The tunnel remains ephemeral. P2 is described
separately as implementation-complete but blocked on a delayed billing row.

**Status:** complete; the stale public-timeout claim was removed without
turning the ephemeral tunnel or incomplete P-2 gate into stronger claims.
