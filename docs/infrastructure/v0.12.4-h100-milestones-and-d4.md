# v0.12.4 — Infrastructure H100 milestone smoke and D4

`vf train` remains the only E1/M3/M4 launch path and retains import preflight,
tmux detach, PGID recording, and W1 cleanup. E1's timebox is 30 minutes. At
T+15 without `Loading safetensors`, capture the approved process/socket/py-spy
evidence; only then may `SIGABRT` expose Python fault-handler output. A
`py-spy` EPERM is recorded verbatim, never treated as a stack.

Every run writes `evidence/runtime-environment.txt` before its GPU child:
`pip freeze`, `nvidia-smi` driver/GPU output, and the diagnostic environment
presence. M6 synchronizes logs, evidence, report, curve, and small manifests
only; checkpoints and weights remain on `/workspace`. SHA-256 comparisons are
recorded after sync.

**E1 implementation:** `runtime-environment.txt` is created atomically before
the driver process starts, and `vf train` retains its existing import-preflight,
tmux/PGID, and W1 lifecycle controls.
