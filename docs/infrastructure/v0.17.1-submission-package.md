# v0.17.1 submission-package infrastructure note

**Status:** completed, 2026-07-17.

The sole infrastructure action is a single GitHub CLI attempt to make the
repository private. Authorization failures are not retryable in this wave:
record the raw response in the run sheet as `OWNER-ACTION` and direct the owner
to **Settings → Danger Zone → Change visibility**. This note does not alter
SSH, GPU pods, object storage, or running services.

The static reviewer path must use tracked artifacts and
`VF_API_DATA_MODE=artifacts`, which avoids credentials and mutable run data.

The one actual GitHub mutation succeeded and a follow-up read reported
`PRIVATE`; no owner action is pending for repository visibility.
