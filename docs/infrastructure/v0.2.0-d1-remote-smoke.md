# v0.2.0 — Infrastructure: D1 Remote Smoke

## Planned Change

Prepare the RunPod workspace and remote control plane for a detached fake-training smoke test. `vf bootstrap` must create or update `/workspace/verifierforge` through the read-only deploy key, install `git`, `tmux`, `rsync`, and Python venv support as needed, install `requirements-trainer.txt`, and create `HF_HOME=/workspace/hf-cache`. `vf status` must show tmux sessions and `nvidia-smi`; `vf watch` must exclude `ckpt/` and `*.safetensors`.

## Acceptance Evidence

Record the exact non-secret commands and outcomes for bootstrap, `vf train`, `vf logs`, `vf watch`, `vf status`, and cleanup. Confirm no checkpoint or model artifact enters Git or the watch sync, and that all persistent paths remain under `/workspace` on the pod.
