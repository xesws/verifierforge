# v0.16.2 S3 lifecycle audit repair

**Status:** completed and pod-verified.

`injected` means a tmux session received credentials through stdin-derived
environment variables; it does not record values. `cleared_by=vf_kill` is
written only after the control plane has verified that the job process group,
tmux session, and GPU allocations are gone. It is an audit completion record,
not a claim that a kill occurred at a particular training step.

The H100 cleanup verification observed no tmux session and 0 MiB GPU memory
before the marker was written.
