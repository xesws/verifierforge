# v0.8.0 — Branch A execution order

After v0.8.0 is pushed, the pod checkout is fast-forwarded and its existing
loopback-only vLLM service is left unchanged. A2 runs the offline re-verifier
first; it has no network/model dependency and writes its evidence under ignored
`runs/p0-gate-a/`. Only a 276/276 result starts a detached `k=8` subset Gate A
against `127.0.0.1:8000` with only `VF_EVAL_BASE_URL` and `VF_EVAL_MODEL`.

The rerun's model samples and evidence are synced back with hashes checked. No
OpenRouter key or `.env` moves to the pod, and infrastructure exit `2` still
uses the existing two-consecutive-run stop gate.

## A3 execution result

The pod fast-forwarded to `0d606df`, confirmed the loopback vLLM model endpoint,
and completed the detached `vf-gate-a-v080` session. Its exit was `1` from the
fixed Gate A threshold, not an infrastructure failure. The laptop and pod
sample/evidence SHA-256 values matched after rsync; no external endpoint or
secret was used.
