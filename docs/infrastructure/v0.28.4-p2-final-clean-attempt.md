# v0.28.4 — final clean P-2 attempt

The failed `-r3` pod `wuvk037vks2c57` is absent and raw `vf-auto-*` inventory
is zero. Its S3 prefix contains 49 metrics plus a complete checkpoint failure
artifact and no published checkpoint manifest. Those objects remain immutable
failure evidence.

The final attempt uses a new `-r4` job/S3 prefix, five-minute monitoring, the
same on-demand GPU ordering/Secure fallback, and the cumulative `$5` plus
180-minute fuses. It does not repeat gold or orphan work. Failure stops the
wave; success must collect and verify all S3 outputs before deletion/tagging.
