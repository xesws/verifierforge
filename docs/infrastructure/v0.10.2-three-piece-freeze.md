# v0.10.2 — immutable tag and detached Gate B procedure

The U3 manifest is generated locally from the frozen data/evidence, committed,
and then annotated as `v0.10.2-p0-three-piece-freeze`. The tag annotation names
the evaluated local model but contains no credential. `main` and the tag must
both reach origin before RunPod starts work.

The pod checks out the exact tag commit, then stops `vf-eval-vllm` and verifies
that its vLLM process/GPU allocation is gone before `vf train` detaches Gate B.
The existing `vf` preflight import remains mandatory. Poll the detached job no
more often than every 120 seconds; preserve logs/artifacts on any failure.

**F1 result:** local manifest publication and fixture-alias verification passed;
tag/push and evaluator release remain the next gated infrastructure actions.

**F2/F3 start result:** tag `v0.10.2-p0-three-piece-freeze` was pushed and
RunPod confirmed `HEAD=ad3d36b` before evaluator shutdown. Its tmux vLLM,
process list, port 8000 listener, and GPU compute list were empty before
`vf train p0-gateb-v102 grpo_v1_0p5b` detached successfully.

**F3 completion result:** after the required two-minute polling cadence, the
`p0-gateb-v102` tmux session had stopped with 100 metric lines, checkpoints at
50 and 100, and final curve/manifest artifacts. The laptop rsync copy matches
the pod metrics SHA-256
`8fae5fffbed41ef70a2dda696a7c8fdfa6c73a9888abc6f1bc7a86eb9f1e2043`; it also
matches the synced curve and final-model-manifest hashes recorded in the
run-sheet. The log retains its DataLoader-worker teardown traceback alongside
the wrapper's successful final-validation/checkpoint/`Finished` sequence; no
automatic retry or training-setting change was made.
