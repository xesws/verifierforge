# v0.9.0 — local-vLLM difficulty probe execution

The existing loopback vLLM process stays on `127.0.0.1:8000`. After the v0.9.0
commit is pushed and the pod is fast-forwarded, B1 runs in detached tmux with
only `VF_EVAL_BASE_URL` and `VF_EVAL_MODEL`; it unsets all augmentation and
eval-key variables. Status is checked no more often than every two minutes.

After completion, artifacts sync to ignored local `runs/p0-gate-a/` and hashes
must match before B2. The offline construction/projection/reverification tools
never open a provider connection. A failed B1/B3 execution writes normal
failure evidence and follows the existing exit-2 circuit-health rule.

## B3 execution result

The `vf-b3-v090` tmux job finished normally with exit `1` from Gate A, not an
infrastructure error. Both the 400-sample JSONL and the completed evidence
file synchronized with matching hashes. The pod-local vLLM service was the
only completion endpoint used.
