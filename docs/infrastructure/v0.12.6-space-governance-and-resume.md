# v0.12.6 — H100 space recovery and resume operations

S1 froze the stopped state: no tmux session, 150 bridged metrics, and Storage
steps 50/100. S2 deletes successful staging material and the explicitly
authorized old D4 checkpoint/staging trees while retaining their metrics and
logs. The current failed step-150 staging tree is moved into job evidence, not
deleted.

After recovery, report `df -h /workspace` verbatim. The shared mount's free
space is not a usable quota guarantee: the host reported `[Errno 122] Disk
quota exceeded` despite large `df` free space and no `quota` command. The new
preflight therefore protects filesystem capacity but cannot prove a provider
quota; the initial resumed checkpoint is watched through the next publication.

S2 retained the old job's metrics/log while deleting only its checkpoint and
staging trees. The shared mount still reported `364T` total, `229T` used, and
`135T` available after cleanup, so that output is recorded as filesystem state,
not proof that the per-pod quota is available. Each admitted run now writes its
budget arithmetic before starting the GPU child.

The post-S3 H100 check found no tmux server but an orphaned 2,134 MiB GPU
allocation (`PID 2743762, [Not Found], 2118 MiB`). The operator explicitly
classified this as a host-namespace residual (~2.6% of 81,559 MiB), accepted it
for this run, and prohibited `nvidia-smi --gpu-reset`. The resumed job must be
watched through step 150; CUDA errors, OOMs, or anomalous throughput require
`vf kill` and a stop report rather than an automatic reset.

That guard passed at the resumed step-150 publish: no CUDA/OOM was observed,
the measured progress was 15.17 seconds/step (about 3.95 steps/minute), and
the public metric stream advanced from the historical 150 to 151--155. The
accepted ghost allocation did not block FSDP, vLLM, checkpoint publication, or
Storage retention.

M3 later completed through step 400: tmux exited, the targeted
CUDA/OOM/bridge/entropy scan remained empty, and the GPU returned to the same
idle 2,134 MiB host-namespace residual at 0% utilization. This is the
operator-approved post-M3 baseline for the serial M4 launch, not a reset target.

Do not delete the model cache, frozen data, metrics, logs, runtime evidence, or
failed-checkpoint evidence. M4 starts only after resumed M3 completes and
releases its own GPU work; the known host-namespace residual is explicitly
accepted for this run.

The serial M4 control has now completed its 200 scheduled steps and released
its own GPU work, returning to the same approved 2,134 MiB residual. Its four
Storage publications and final artifacts exist. Preserve its train log: the
Ray DataLoader-worker `Killed` traceback is a post-completion teardown record,
not a reason to erase evidence or reset the host GPU.

M5's loopback server for the first held-out checkpoint exited at model load and
the evaluator published an unavailable report. Its tmux session is down and no
vLLM server remains; GPU use is back at the operator-approved 2,134 MiB residual.
Do not reset, retry, or start M6 from this state.
