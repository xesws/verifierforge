# v0.18.0 — LLM cost and model guard

The owner-declared OpenAI account balance is $10. A-0 and Gate C share a hard
$3 run budget and preserve a $7 reserve; OpenRouter has a separate $2 ceiling.
Before each paid call the caller reserves a conservative maximum cost from a
frozen price table. A request is refused when the reservation would exceed its
provider ceiling. Provider-reported cost is preferred; otherwise the report
labels token-derived cost as an estimate.

The OpenAI execution policy is fail-closed: `gpt-5.6-luna-xhigh` is the only
formal model for this wave, while `sol` and `terra` are forbidden before the
transport is invoked. A model failure never authorizes an automatic upgrade.
No GPU, pod, trainer, or provisioner operation is part of this version.

The two A-0 attempts consumed $0.50 of conservative ledger reservation
($0.25/provider); provider-reported cost was unavailable. This leaves $2.75 of
the approved OpenAI run envelope by the fail-closed accounting rule and makes
no claim about the external billing dashboard.
