# v0.12.0 — Infrastructure: fresh H100 executor rebuild

N1 sets the local `runpod` SSH alias to `157.66.254.39:14694` with the provided
root user, identity key, ControlMaster/ControlPersist, and keepalive settings.
Acceptance requires exactly one H100 80 GB and an empty `/workspace` (or only
`lost+found`); a populated old volume is a hard stop.

N2 creates a pod-local ED25519 key, adds its public half as a read-only GitHub
deploy key (never `--allow-write`), verifies GitHub authentication, and places
the pod key on the new persistent volume only so the existing bootstrap restore
logic can copy it to a root-only `0600` runtime key. No laptop private key or
environment file moves to the pod.

N3 runs the idempotent `vf bootstrap`; N4 snapshots only the two approved
public Qwen models under `/workspace/hf-cache`; N5 verifies the pushed freeze
tag and exact data hashes. N6/N7 use the existing `vf` import preflight,
detached tmux/recorded PGID lifecycle, Storage publication, `vf watch`
synchronization, and W1 `vf kill` GPU-zero assertion. Any gate failure stops
the sequence before the next numbered step.

**N4 result:** the new `/workspace/hf-cache/hub/` contains only the approved
Qwen snapshot families for this task: 0.5B revision
`7ae557604adf67be50417f59c2c2f167def9a775` and 1.5B revision
`989aa7980e4cf806f80c7fef2b1adb7bc71aa306`. Both passed required-file and
nonempty-weight checks; total cache use is 3.9 GB.

**N5 result:** `git fetch --tags` made the freeze tag reachable on the fresh
pod, and both remote dataset digests match the committed three-piece freeze.
This is a data identity check only; it does not move data or copy any local run
artifact to the pod.

**N6 launch boundary:** after tests and a push, `vf train n6-h100-smoke
grpo_v1_1p5b_h100_smoke` must pass the remote module-import preflight before
detaching. Metrics are watched through Storage; after at least 20 nonempty
rows, W1 `vf kill` owns tmux, process group, Ray cleanup, and the 0-MiB GPU
assertion. No direct SSH training launch is permitted.

The target and shell entrypoint passed local syntax and test validation before
the pod is asked to pull them.
