# v0.28.3 — P-2 final-attempt operations

The v0.28.2 replacement `33rpq7bee28z9f` was deleted after the step-50 serving
gate failed. Provider readback showed the target absent and zero `vf-auto-*`
resources. The failure was not model conversion: vLLM loaded one safetensors
shard, then rejected negative KV-cache capacity caused by the 0.10 ceiling.

Before the one allowed `-r3` create, the controller must prove raw prefix zero,
old target absence, a complete database audit chain, clean pushed HEAD and an
empty `vf/jobs/<job>-r3/` prefix. Monitoring remains five-minute cadence. The
same on-demand GPU order, Secure capacity fallback, 180-minute runtime fuse and
combined `$5` ceiling apply.
