# v0.12.3 — Infrastructure Gate B replay diagnostic

The control runs in a detached tag worktree on the H100 but writes all runtime
state to `/workspace/verifierforge/runs/c1-gateb-replay-v0123/`. The evidence
directory must atomically retain the tag config, SHA-256 records, PID manifest,
`py-spy` install/version output, and complete native WorkerDict stack.

W1 owns teardown after the metric/no-metric branch decision or the fifteen
minute timeout. It must prove the tmux session and process group are gone and
the GPU is at 0 MiB. No external model API or secret is used.

**C1 result:** the control completed eight metric records, then W1 removed its
tmux session and job processes and restored the H100 to 0 MiB. Pod/laptop
metrics, train-log, and WorkerDict-dump artifact hashes match. The dump itself
is an honest failed capture: the container lacks Docker `SYS_PTRACE`, so
`py-spy` reports `Permission Denied` for both the driver and WorkerDict. A
future stack-required diagnostic must provision that capability at pod launch.
