# v0.26.2 — Owner handoff

Two external inputs remain. First, restore/expose the `vfserve` endpoint and
rerun `python -m scripts.public_endpoint_proof`; only a successful public proof
permits the 200-request canary/guardian closeout. Second, copy a resolvable
Supabase transaction-pooler PostgreSQL DSN into `SUPABASE_DB_URL`, then execute
the migration and rollback-owned Postgres contract commands in
`v0.25.0-supabase-migration.md`. Neither blocker weakens
DB-1, DB-3, or P-1, and neither milestone may be claimed before its own gate.
