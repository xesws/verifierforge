# v0.4.1 — Infrastructure: Persistent TransferQueue Install

Install `TransferQueue==0.1.7` with the pod's
`/workspace/verifierforge/.venv/bin/python`, using the persistent pip cache.
The pin lives in `requirements-trainer.txt`, so future idempotent bootstrap runs
restore it. No CUDA runtime or model cache is changed by this patch.
