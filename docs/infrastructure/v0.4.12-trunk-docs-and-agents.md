# v0.4.12 — Trunk Convergence Notes

**Status:** Complete

Records the branch/worktree cleanup that left only `main` at the primary
checkout, the addition of `assets/` to `.gitignore`, and the trunk-first /
`core/` serialization / merge-cleanup rules written into `AGENTS.md`.
