# v0.12.2 — Infrastructure H100 smoke gate result

The N6 job was detached through `vf`, then stopped through W1 after 11 minutes
without a metric artifact. Teardown removed tmux and the job process group and
left the H100 at 0 MiB. The remote training log remains in the persistent
`/workspace/verifierforge/runs/n6-h100-smoke/` directory for a future,
separately authorized diagnosis.
