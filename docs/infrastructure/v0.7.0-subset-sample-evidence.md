# v0.7.0 — local-only subset diagnostic rerun

**Status:** implementation validated; diagnostic rerun pending. Linked plan:
`docs/versions/v0.7.x/v0.7.0-subset-sample-evidence.md`.

The diagnostic runs detached in pod tmux against the already-running loopback
vLLM service at `127.0.0.1:8000`. It clears `VF_LLM_*` and
`VF_EVAL_API_KEY`, then sets only `VF_EVAL_BASE_URL` and `VF_EVAL_MODEL`.
Persistent outputs are placed under `/workspace/verifierforge/runs/p0-gate-a/`
and synced back only after completion.

The initial job is exactly 50 records × 8 samples. Its tmux command exits with
the reference evaluation result; it makes no external HTTP request. After the
artifact is verified, the runbook routes automatically using `D` and continues
only through the selected branch's documented stop condition.
