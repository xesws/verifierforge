# v0.6.2 — verifier and eval-evidence binding

**Status:** in progress. Linked plan: `docs/versions/v0.6.x/v0.6.2-pod-eval-recovery.md`.

`NL2SQLVerifier` remains unchanged. It still admits S1/S2 data only at exact
score `1.0`, and Gate A uses the same verifier for each model completion.

The freeze manifest consumes only completed v2 Gate A evidence that matches the
input data and verifier source hash. It records the common eval model and
sanitized loopback endpoint alongside the full/subset hashes, selection facts,
and verifier Git blob identity.
