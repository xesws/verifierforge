# v0.10.2 — verifier v2 freeze identity

U3 does not alter `NL2SQLVerifier`. The freeze manifest records its stable
identity, `VERSION`, source SHA-256, and Git blob ID, and re-verifies every
training and held-out `reference_sql` at final score `1.0` before publication.
Any mismatch or reference regression blocks the tag and Gate B.

**F1 result:** all 110 frozen reference SQL values scored `1.0` under v2;
source SHA-256 is `b76707e5c0603d227393d03ffc0bdc7c66ec072a139c6aa717c282f4082de47f`
and Git blob ID is `b91d3872feab82d3ef29715746935149eed11efa`.
