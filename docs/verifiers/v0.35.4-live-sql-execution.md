# v0.35.4 — Live execution verifier boundary

The reviewer sandbox uses the schema and INSERT fixture from
`data/nl2sql/v0.10.0-training-pool.jsonl`. The canonical source SHA-256 is
`c97a5adea789fae3be249bc9ac95a1902ae5a9769de9eefbc08277f056878e8c` and the
shared schema SHA-256 is
`688cfaf8a4fff6743b541dcfec2c2de10793232458342c8160178394c510631d`.

The browser asset contains only this schema/fixture. It must not expose or use
the training rows' `reference_sql` or `expected_results`. A deterministic
check fails closed if the canonical pool stops containing exactly one schema
or either hash changes without an explicit version update.

Live execution is not `NL2SQLVerifier.score()`. A successful query proves that
one read-only statement ran against the same bounded schema and reports its
actual rows. It does not prove that those rows answer an arbitrary natural-
language question, because such a question has no frozen expected result.
`NL2SQLVerifier.VERSION == 2` and all existing reward behavior remain unchanged.

The deterministic asset check and full repository suite passed with
`479 passed, 1 skipped`; no verifier source or frozen dataset file changed.
