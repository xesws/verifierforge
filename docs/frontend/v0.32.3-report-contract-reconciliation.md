# v0.32.3 frontend report reconciliation

**Status:** complete — 2026-07-20

The report page receives a non-null ten-card arena, a non-null savings value,
the formula and assumptions behind that value, and source provenance. Existing
field names remain intact. The arena selection is deterministic: six tuned
wins, two both-pass cases, and two both-fail cases from held-out data only.

The frozen frontend operation count is reconciled to 19. The integration guide
will distinguish those formal operations from internal/debug routes and state
the expected disabled behavior for Start Forge.

Frontend acceptance is structural equality of the report across artifacts,
hybrid, and Supabase modes; no mode may return null for arena, savings, or
verdict.
