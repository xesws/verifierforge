# v0.35.5 — Arena pass@1 copy correction

The Proof arena distinguishes the evaluation population from the visual
sample. The pass@1 result is computed across all 60 unseen held-out prompts;
the page displays ten deterministic side-by-side comparisons from that set.
The model training pool contains 50 prompts.

This is a copy-only correction. It does not change report values, sample
selection, API contracts, or the evaluation methodology.

The corrected production copy is deployed at the stable Vercel alias. The
focused page-flow regression, lint, build, and full repository suite passed.
