# v0.39.4 — `/tech` Agent figure correction

The `/tech` page continues to consume the generated Technical Deep Dive mirror.
No React or CSS behavior changes. Regenerating the canonical Figure 2 updates
the mirrored SVG bundled by Vite and preserves the existing responsive image
behavior, accessible title/description, and `/tech` route.

Acceptance is a production build plus deployed visual inspection confirming
that Gate C belongs to the Agent evaluator and not to the human/Provisioner
side of the approval wall.

Local acceptance completed with 35 Vitest checks, ESLint, and the Vite
production build. The deployment target remains
`https://verifierforge-web.vercel.app/tech`.

Production deployment `dpl_3Je5EuZjMHsxZ41FoecRzX5z4tMd` reached Ready on
2026-07-21. The public route returned HTTP 200 and served the exact locally
validated Figure 2 asset by SHA-256.
