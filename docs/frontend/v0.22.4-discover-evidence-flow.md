# v0.22.4 — Data Pull SQL Discover presentation

The Data Pull SQL card shows `SQL Volume · 95,000 queries/mo`, `Monthly Cost ·
$5,500/mo`, and an Input control before analysis. The Input panel is prefilled
with the server's configured traffic database; users must confirm it before
Analyze becomes available.

A successful Forge result opens a full-width detail panel with three explicit
steps. Solution shows recommendation, rationale, confidence, and cache status.
Schema renders the Agent's six-field `TrainingConfig` in human-readable form.
Approve requires acknowledgement of the side-effect boundary before retaining
the product label `Approve & Forge`.

The approval receipt shows its identifier, approver, timestamp, `No GPU
allocated`, and `No training started`. Support and Invoice keep their compact
cards. The page remains unavailable while `VF_AGENT_ENABLED=false`.

## Validation result

Desktop interaction verified Input confirmation, Analyze, all six proposed
schema fields, acknowledgement gating, and the persisted approval receipt. A
390px viewport completed the same evidence-to-detail transition without
horizontal overflow. Three screenshots are committed under
`assets/forge-agent/`.
