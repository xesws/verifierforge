# v0.34.1 Cold-state reviewer experience

**Status:** in progress

Discover must visibly distinguish `provisioning`, `loading`, `ready`, and
failure. While cold/loading, the three cards, flagship report, curves, arena,
savings, routing, and LivePassRate remain readable. A tuned proxy request in
cold state returns a successful default fallback with
`X-VerifierForge-Route: default-fallback`; the UI never waits on or assumes the
retired L4.
