# v0.34.1 Cold-state reviewer experience

**Status:** complete

Discover must visibly distinguish `provisioning`, `loading`, `ready`, and
failure. While cold/loading, the three cards, flagship report, curves, arena,
savings, routing, and LivePassRate remain readable. A tuned proxy request in
cold state returns a successful default fallback with
`X-VerifierForge-Route: default-fallback`; the UI never waits on or assumes the
retired L4.

The hosted page now supplies explicit copy for provisioning, model loading,
ready, draining, cold, and failed wake outcomes. Public cold-state acceptance
returned three Analyzer-backed cards, 400/200 main/control points, ten held-out
arena samples, `$3,850` savings, `real_gain`, routing and 143 Guardian points
ending at 0.95. Concurrent hydration reduced the three-card response from
about 9.4 seconds to 2.5–2.8 seconds.
