# v0.12.7 — M5 serveable-export rerun

The prior M5 report is preserved as unavailable evidence: step 50 failed before
any completion because vLLM could not load a `base_model` weight key. It must
not be overwritten or used as an after metric.

X3 passed: the converted step-50 directory returned HTTP 200 from both
`/v1/models` and a real completion endpoint request; its complete atomic
evidence has SHA-256
`e83e20ae11eeb7ce863851aeaf3afda207581183fc63c4cb7072fda029986039`. M5
must select those serveable paths rather than the original source exports. It
runs one local server at a time, uses the frozen 60-row held-out file at `k=8`,
and writes 480 full sample/tier records per checkpoint. The report contains all
eight triplets, SHA-256 identities, the M4 control-curve hash, fixed before
`0.5833333333333334 / 0.7666666666666667 / 0.4666666666666667`, and the
deterministic best-checkpoint rule.

If the selected after pass@1 is below the before pass@1, stop and ask the human.
Otherwise the report may be archived in X5. Training-pool and random-control
numbers remain non-evaluative throughout.

X1 confirms the source is a full PEFT-wrapped state, not a standalone adapter.
M5 must therefore consume the merged standard-HF sibling created by X2; pointing
vLLM at the raw `base_model`/`base_layer` export is known to fail and is never a
valid retry.

X4 completed all 3,840 samples. It selected step 350 with
`0.7833333333 / 0.9 / 0.4333333333`, ahead of before
`0.5833333333 / 0.7666666667 / 0.4666666667`; its report SHA-256 is
`ebe29fb19d02ac3072ff09a0f1756d18373bfb1d1eb4d123f07457e9b9b5ceb8`.
X5 therefore archives this report and the per-checkpoint evidence rather than
re-running an evaluation. The future serving gate uses the same two endpoint
proof (`/v1/models` plus one completion) before a checkpoint is publishable.
