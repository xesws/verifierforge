# v0.15.1 — Evaluation/serving provenance

The held-out result `0.7833 / 0.9000 / 0.4333` remains tied to the canonical
step-350 standard-HF tree digest `7bde…`. This patch adds provenance detail;
it does not rerun evaluation or revise metrics. Serving remains prohibited
until the canonical identity check passes.

**Result:** the canonical replay passed, so no duplicate held-out evaluation
was needed and no serving process was started during arbitration.
