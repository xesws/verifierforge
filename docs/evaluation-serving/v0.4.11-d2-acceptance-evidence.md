# v0.4.11 — Evaluation Serving: D2 Acceptance Evidence

**Status:** Complete

## Scope

Record that verl file metrics became append-only `MetricRecord` lines and that
the local API aggregated the synced file into the existing `Metrics` shape.

## Required Evidence

- The public JSONL has unique, ordered steps 1 through 100 after resume.
- The local `/jobs/d2demo/metrics` response has equally sized `steps`,
  `reward_mean`, `pass_at_1`, and `entropy` arrays.
- `artifacts/final/model.txt` and `artifacts/curve.png` exist without moving
  model weights out of checkpoint storage.

## Non-goals

No database, new API contract, dashboard, or quality report is introduced.

## Observed Evidence — 2026-07-15

The local public JSONL had exactly 100 ordered, unique steps from 1 through 100
after the kill/resume run. The resume did not rewrite the existing rows for
steps 51 and 52; its first newly appended row was step 53. `GET
/jobs/d2demo/metrics` on the local port-8010 API returned four aligned arrays
of length 100. `artifacts/final/model.txt` and `artifacts/curve.png` synced;
the manifest states that weights remain in `ckpt/`.
