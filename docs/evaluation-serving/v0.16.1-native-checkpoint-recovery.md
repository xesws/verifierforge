# v0.16.1 native checkpoint recovery — evaluation/serving boundary

**Status:** native recovery passed; timed node-loss proof incomplete.

S7 measures durable training recovery, not serving compatibility. The existing
strict vLLM publication gate correctly rejected a non-safetensors 0.5B export;
that evidence remains preserved. The recovery tool may publish its complete
native model/optimizer/RNG payload to S3 for the node-loss exercise, but it does
not call it a serveable checkpoint and does not affect the selected D4 serving
export or held-out evaluation evidence.
