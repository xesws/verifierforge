# v0.12.5 — Held-out completion gate

M5 remains the sole source of before/after claims. It begins only after the
fresh 400-step M3 job completes and releases the H100; the old v0.12.4
step-50/100 exports are retained as failure evidence and are not candidates.

The evaluator serves each eligible fresh M3 HF export on pod loopback, runs the
frozen 60-row held-out set at `k=8`, and retains all 480 completion/tier rows.
It selects maximum held-out pass@1, breaking ties by lower checkpoint step.
The fixed before snapshot is `0.5833333333333334 / 0.7666666666666667 /
0.4666666666666667` for pass@1 / pass@8 / mixed fraction. If after pass@1 is
below the before value, report the full evidence and stop instead of proceeding
to a gain claim.

**Run disposition:** M5 did not start. The fresh M3 Storage gate failed at
step 150 and did not publish a final artifact or complete 400-step candidate
set. Its step-50/100 exports remain failure evidence only and are not eligible
for held-out selection or an after/gain claim.
