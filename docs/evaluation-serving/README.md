# Evaluation and Serving Documentation

This area owns baseline and control evaluation, pass@1 reporting, verdicts, narrative generation, final artifacts, model registry, and OpenAI-compatible serving.

Before work begins, create a versioned document that names the baseline, control comparison, report fields, artifact locations, endpoint behavior, rollback plan, and proof required for the demo. Coordinate contract changes with `docs/backend-api/` and storage implications with `docs/infrastructure/`.
