# v0.17.0 — evaluation evidence shipped with the repository

The delivery dataset copies only D4 public metrics and the completed held-out
report. The selected step is 350 by the committed rule: maximum held-out
pass@1, lower step on ties. The report preserves the complete before/after
triplets and checkpoint table; it does not ship model weights or a fresh score.

The actual public serving proxy remains blocked as recorded in v0.15.2, so no
artifact represents it as live. Local vLLM serving passed and is documented as
such, separately from public reachability.
