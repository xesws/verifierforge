# Provisioner documentation

This area records versioned work on provider-neutral GPU lifecycle contracts,
adapters, orchestration, safety fuses, BYO credentials, and approval-to-execute
integration. P-1 is mock-only; live RunPod/Nebius adapters belong to later
stages and require explicit owner authorization.
