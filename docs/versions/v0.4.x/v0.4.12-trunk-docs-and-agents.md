# v0.4.12 — Trunk Docs and Agents Rules

**Status:** Complete

**Owner/worktree:** trunk on `main`
**Type:** Documentation and infrastructure patch

## Problem and Scope

After D2 branch sprawl, converge to trunk-first development and land the
parked project bible (`docs/dev_doc_v0.md`) plus `assets/` ignore and
long-term merge rules in `AGENTS.md`.

## Non-goals

- No `core/`, trainer, API, or mock behavior change.
- No contract or frontend fixture change (those land in `v0.5.0`).

## Affected Areas and Contract Impact

- `.gitignore`, `docs/dev_doc_v0.md`, `AGENTS.md`, this version document.
- No API or shared-contract change.

## Validation and Merge Gate

1. `pytest -q` remains green on `main`.
2. Stale feature/fix/docs/integration branches and worktrees are removed.
3. Half-finished local files are committed, not discarded.
