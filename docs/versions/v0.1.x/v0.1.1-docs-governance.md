# v0.1.1 — Documentation Governance

**Status:** Ready for merge

**Owner/worktree:** integration worktree on `main`

**Type:** documentation-only patch

## Scope

Establish the version naming, worktree merge, and documentation-first rules in `AGENTS.md`. Create the `docs/` area map and version-document convention so all subsequent implementation has a pre-written release plan and affected-area documentation.

## Non-goals

No runtime behavior, contracts, trainer logic, or API behavior changes.

## Affected Areas

- `AGENTS.md`
- `docs/README.md`
- `docs/versions/`

## Validation and Merge Gate

Confirm every declared area exists, filenames follow the version convention, and `git diff --check` passes. Merge only with this document and the documentation map in the same change.
