# v0.32.3 — Report contract reconciliation

**Status:** complete — 2026-07-20
**Owner/worktree:** Codex on `/Users/tangyiq/dev/verifierforge` `main`
**Release type:** patch contract/data correction

## Problem and scope

The frozen frontend contract exposes a D4 report whose arena and projected
monthly savings are null, while the operation ledger documents 19 operations
but freezes only 16. This patch deterministically derives the complete report
from the held-out M5 evidence, makes the report projection reproducible across
artifact, hybrid, and Supabase data modes, and reconciles the public operation
count.

## Non-goals

No trainer, frozen dataset, model weights, GPU process, provider provisioning,
paid LLM call, or destructive contract change. Supabase projections remain a
derived presentation cache; artifacts/S3 remain authoritative evidence.

## Affected areas and contract impact

- `core/contracts.py`: additive savings and projection-provenance models only.
- Artifact building/API/mock: deterministic 10-sample held-out arena, savings,
  projection backfill, and three-mode parity.
- Frontend/backend/database docs: 19-operation reconciliation and explicit
  data-source policy.

Existing report fields remain available. New fields are additive, and the
existing `projected_monthly_savings_usd` scalar remains populated for frontend
compatibility.

## Validation plan

Prove deterministic arena selection and hashes; exact `$3,850` monthly savings;
artifact/hybrid/Supabase report parity; 19 documented/frozen/real-mock operation
parity; full pytest, secret scan, compilation, and diff checks. Stage only
explicit paths so the two pre-existing provisioning billing files stay out of
the commits.

## Result

The shipped projection is 24,775 bytes, uses ten deterministic held-out cards
(6 improved / 2 both-pass / 2 both-fail), and reports population win rate
`0.20` plus `$3,850` projected recurring savings. Artifact, hybrid, and strict
Supabase reads return the same report; the database projection SHA is
`2d5d919148e3f9cb54972b23cadaebc57d0449061bfe924e942b59020a7b5326`.
The frontend boundary now has 19 documented, frozen, and real/mock-parity
operations. Validation: `449 passed, 1 skipped`; secret and diff checks passed.
