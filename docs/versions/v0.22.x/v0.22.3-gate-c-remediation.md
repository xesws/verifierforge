# v0.22.3 — Gate C failure taxonomy and remediation

**Status:** complete; live Gate C passed in remediation round 2
**Owner/worktree:** Codex, repository `main`
**Stage:** Forge Agent Gate C remediation, at most three live rounds

## Problem and scope

The v0.22.2 Responses transport worked, but live Gate C scored
`0.6666666666666666 / 0.8333333333333334 / 0 / 0.5`. Reconstruct every
scenario from durable traces, classify failures as formatting, intelligence, or
dependency-chain defects, then repair only the evidenced layers.

Formatting repairs bind `submit_decision` to a strict Responses function schema
and align parsing. Intelligence repairs make decision criteria explicit and add
one general example for each decision class without naming frozen scenarios.
Chain repairs preserve issued dependency IDs between tool turns.

## Non-goals

Do not change frozen scenarios, gold labels, thresholds, model, provider,
feature-flag state, tool business outputs, or training code. The only budget
change is the explicit owner authorization below.
Do not hard-code scenario IDs or answers. Do not issue a live batch until replay
passes after each remediation round.

## Affected areas and contract impact

- `app/agent/runner.py`: strict terminal schema, decision rubric/few-shot, and
  dependency-context handling.
- `app/gpt/client.py`: no transport change is required by T1/T2; the existing
  Responses function-call path remains intact.
- `app/gpt/budget.py` and `scripts/agent_gate_c.py`: the owner-authorized
  cumulative OpenAI ceiling changes from `$3` to `$8`, leaving `$2` of the
  declared `$10` credit untouched; each of at most three v0.22.3 rounds
  reserves `$1.50` conservatively.
- `app/agent/evaluator.py`: thresholds and the preflight gate remain unchanged.
- `tests/`: strict schema, prompt, dependency, replay, and live-admission guards.
- `AGENTS.md`, `docs/forge-agent/`, `docs/gpt-integrations/`, and run-sheet:
  durable remediation and retry rules.

No existing public Pydantic field is renamed or removed.

## Validation and stop conditions

First publish a 12-row read-only failure table. Before each paid round run the
zero-cost replay evaluator and require it to pass. Preserve the mandatory
two-probe flight check. Permit at most three live rounds for v0.22.3 under an
additional `$5` authorization and `$8` cumulative conservative cap. Thresholds
remain `0.90 / 0.90 / 0 / 1.00`. Tag `agent-gate-c-pass` only on a full pass;
never enable the feature flag automatically. After three failures, stop with the
per-scenario table.

## Result

Round 1 scored `0.9166666666666666 / 1.0 / 0 / 0.75`; the remaining failure
was a general evidence-precedence error, not a chain or parser defect. After
clarifying that explicitly labeled trusted evaluator facts are authoritative,
round 2 scored `1.0 / 1.0 / 0 / 1.0`. Both paid rounds followed a passing
replay and the two-probe preflight. Conservative OpenAI ledger usage ended at
`$6.00` of the `$8.00` owner cap; provider-reported cost was unavailable.
`agent-gate-c-pass` marks the verified commit. The feature flag remains off.
