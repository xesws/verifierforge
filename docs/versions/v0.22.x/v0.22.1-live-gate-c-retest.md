# v0.22.1 — Live Gate C model discovery and retest

**Status:** complete; live Gate C did not pass
**Owner/worktree:** Codex, repository `main`
**Stage:** Forge Agent A-3 live admission retest

**Discovered model:** authenticated `/v1/models` returned `gpt-5.6-luna`,
`gpt-5.6-sol`, and `gpt-5.6-terra`; the selected exact ID is
`gpt-5.6-luna`.

## Problem and scope

The first live Gate C attempt used an unverified remembered model ID and received
HTTP 404. Query the authenticated OpenAI-compatible `/v1/models` endpoint, record
every returned ID containing `gpt-5.6`, and select the base Luna ID only from
that response. Add a repository rule prohibiting guessed model IDs, make live
Gate C resolve its model from `VF_AGENT_EVAL_MODEL`, then run the frozen scenario
set once within the existing OpenAI budget.

## Non-goals

Do not change the frozen scenarios, evaluator thresholds, Agent contracts,
training code, feature-flag default, or product routing. Do not call Sol or
Terra. A passing tag is evidence only and does not enable the Agent.

## Affected areas and contract impact

- `AGENTS.md`: authenticated model-discovery rule.
- `app/agent/evaluator.py`, `app/gpt/`, and `scripts/agent_gate_c.py`: explicit
  live-eval model resolution and validation.
- `tests/`: resolution and fail-closed policy coverage.
- `docs/forge-agent/` and `docs/p0-run-sheet.md`: raw discovered IDs, metrics,
  spend evidence, and gate decision.

There is no public Pydantic contract change.

## Validation plan and stop conditions

Require `VF_AGENT_EVAL_MODEL` to be one of the IDs observed from `/v1/models`,
run focused tests and `pytest -q`, then run all 12 live scenarios exactly once.
Apply the unchanged Gate C thresholds: decision accuracy `>=0.90`, chain success
`>=0.90`, illegal actions `=0`, config legality `=1.00`. Tag
`agent-gate-c-pass` only if all four pass. Otherwise record the exact numbers
without threshold or scenario changes. Stop before a paid run if discovery,
model policy, or the existing budget guard fails.

## Result

The authenticated model discovery, dedicated environment resolution, permanent
repository rule, and regression tests are complete. The formal 12-scenario run
selected exact model `gpt-5.6-luna` at `https://api.openai.com/v1` with the key
sourced only from `OPENAI_API_KEY`.

The unchanged four-metric tuple was `0.0 / 0.0 / 0 / 0.0` (decision accuracy /
chain success / illegal actions / config legality). All 12 attempts ended before
a tool call with `LLMRequestError`; the durable S3 failure traces each record
zero tokens and zero tool calls. The report therefore contains 12 missing-trace
failures and is not gate-eligible. No retry or threshold/scenario change was
made, no tag was created, and `VF_AGENT_ENABLED` remains false.

The conservative ledger charged the pre-authorized `$2.50` reservation because
the provider reported no cost; this is an upper bound, not evidence that `$2.50`
was billed. Final validation passed 298 tests with one expected skip.
