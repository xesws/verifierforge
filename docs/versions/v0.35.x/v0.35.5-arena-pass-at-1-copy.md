# v0.35.5 — Arena pass@1 copy correction

Status: deployed

## Problem and scope

The Proof arena currently emphasizes ten displayed comparisons without making
it explicit that the reported pass@1 result was calculated across all 60
unseen held-out prompts. Correct the heading and supporting copy so the metric
population and the smaller visual sample are unambiguous.

## Non-goals

- Changing report data, arena selection, scores, contracts, or API behavior.
- Re-running evaluation or training.
- Changing any page outside the Proof arena.

## Ownership and affected areas

- Version: `v0.35.5`
- Branch: `fix/v0.35.5-arena-pass-at-1-copy`
- Owner: primary Codex agent in the main repository directory
- Frontend: `ArenaComparison` wording and its regression assertion.
- Documentation: this version record and the matching frontend note.

## Contract impact

None. The frozen 22-operation HTTP contract and report payload are unchanged.

## Validation plan

- Assert that the rendered Arena copy names pass@1, all 60 held-out prompts,
  the 50-prompt training pool, and the ten displayed comparisons.
- Run the focused frontend test, frontend build, and deploy the patch to the
  existing Vercel production alias.

## Result

The Proof arena now says that pass@1 is measured across all 60 held-out
samples. Its supporting line separately identifies the 50-example training
pool, the 60 unseen evaluation examples, and the ten comparisons displayed.

Validation on 2026-07-20: the focused frontend suite passed 10/10, ESLint and
the production build passed, and the repository passed 479 tests with one
skip. Vercel deployment `dpl_8BW5TKbqMosu6VfChyXZw66di4Ga` reached READY and
was aliased to `https://verifierforge-web.vercel.app`; the public report route
returned HTTP 200 and its served bundle contained both corrected lines.
