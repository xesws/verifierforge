# v0.39.2 — README reviewer and business narrative overhaul

**Status:** complete
**Date:** 2026-07-21
**Owner/worktree:** primary Codex agent on `main`
**Release type:** documentation-only patch

## Problem and scope

The README opens with implementation language instead of the business problem,
mixes the hosted reviewer path with local development, renders an incomplete
ASCII architecture, and stops the Codex engineering log at v0.28.5. This patch
makes the public repository entry understandable without losing the evidence
and operating boundaries that judges need.

The README will explain VerifierForge as a cost-saving system for repetitive
AI work whose outputs can be checked automatically. It will give reviewers a
zero-install Vercel path using the invitation shared through DevPost, separate
that path from contributor setup, replace the ASCII architecture with the
canonical Technical Deep Dive system-loop SVG, and extend the dated Codex log
through the current v0.39.1 deployment.

## Explicit non-goals

- No API, contract, frontend, backend, model, dataset, trainer, deployment, or
  feature-flag change.
- No invitation code, provider credential, database URL, or other secret in
  tracked content.
- No new metric, benchmark, tag, or claim unsupported by committed evidence.
- No regeneration or redesign of the canonical Technical Deep Dive figures.

## Affected areas and contract impact

- `README.md`: public positioning, reviewer guide, architecture, Quick Start,
  and dated Codex collaboration log.
- `JUDGES.md`: narrow consistency corrections for the current 23-operation
  contract and hosted-versus-local cost boundary.
- `docs/submission/`: reviewer-facing wording and evidence-source record.

There is no runtime contract change. Documentation will describe the additive
`POST /serving/sleep` operation already shipped in v0.39.1, bringing the stated
frozen operation count from the stale 22 to the actual 23.

## Validation plan

- Verify the canonical blog article and SVG mirrors are current.
- Run blog-asset and secret-scan focused tests, the secret scanner, and the
  full Python suite.
- Check Markdown-relative paths, public Vercel `/` and `/tech`, Railway
  `/healthz`, whitespace, and the final staged-file set.
- Preserve unrelated runtime logs and local `.DS_Store` files.

## Validation result

- Canonical blog asset check: 17 outputs current.
- Focused blog/secret tests: 8 passed.
- Secret scan: passed.
- Full repository suite: 497 passed, 1 explicitly credential-gated skip.
- Public Vercel `/` and `/tech`, plus Railway `/healthz`: HTTP 200.
