# v0.28.1 — v1 narrative refresh

**Status:** reserved — 2026-07-19
**Release type:** documentation-only patch
**Owner/worktree:** Codex on `main`

## Problem and scope

The reviewer documents still describe an earlier D5 snapshot. They must match
the current repository: Forge Agent Analyze→decision→approval, live Gate C,
Supabase repositories, the successful Cloudflare public canary, the verified
D4 held-out result, and the still-blocked P2 billing-receipt gate.

Update `README.md`, `docs/submission/devpost-draft.md`,
`docs/submission/video-script.md`, `JUDGES.md`, and `docs/dev_doc_v0.md` from
the same evidence sources. The video adds a Discover → Analyze → GPT-5.6 Luna
→ Approve scene. All statements must distinguish durable committed evidence,
ephemeral live proofs, default-off feature flags, and incomplete automation.

## Non-goals

No source code, API contract, data, model, provider resource, feature flag,
training action, or endpoint lifecycle changes. Do not claim P2 completion,
permanent public hosting, or broad model quality.

## Validation

Cross-document fact grep, link/path checks, Markdown inspection, full pytest,
secret scan, and clean Git status. This patch has no milestone tag.
