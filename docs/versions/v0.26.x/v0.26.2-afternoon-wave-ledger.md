# v0.26.2 — Afternoon wave final ledger

**Status:** complete — 2026-07-18
**Owner/worktree:** Codex on `main`
**Release type:** patch documentation closeout

## Problem and scope

Record the final Lane A, DB-1/2/3, and Provisioner P-1 state after the security
workflow passed. Complete DB-2's deferred parameterization seam so the owner
can run the same repository contracts against Postgres inside a rollback-owned
transaction. The run-sheet receives the commit/tag ledger and the two remaining
owner actions with exact resume commands.

## Non-goals

No application, contract, database, endpoint, provider, GPU, trainer, or
frozen-data change. Do not retry either blocked external dependency.

## Affected areas and validation

- `tests/test_repository_contracts.py`: SQLite always; Postgres only when
  `VF_TEST_POSTGRES_URL` is supplied, with table cleanup inside an outer
  transaction that is rolled back after the suite.
- `docs/p0-run-sheet.md`: final status and owner handoff.
- `docs/infrastructure/v0.26.2-owner-handoff.md`: concise resume paths.
- Validate the full local suite, tracked-file secret scan, clean Git state, and
  successful GitHub Actions security run before push.
