# v0.12.3 — Gate B replay diagnostic

**Status:** C1 complete; C2 selected by the predeclared metric rule; no C2
repair authorized
**Owner / worktree:** Codex / repository-root `main`

## Problem and scope

The N6 H100 1.5B smoke reached FSDP `WorkerDict` but never emitted a metric.
C1 isolates hardware/runtime behavior from the new 1.5B configuration by
replaying the completed Gate B workload exactly: the detached tag
`v0.10.2-p0-three-piece-freeze` at commit
`ad3d36b5860c66567a9f60f94c34101c787aaef3`, its original
`trainer/verl_configs/grpo_v1_0p5b.yaml`, and the frozen 50-row input.

The original config's SHA-256 is
`e2b8e973cf2400040dd83684e9bd7f4b0f191390ebce24ee43281f9ed5b81171`; it is
not the current file, which added fields after Gate B. A detached tag worktree
under `/workspace/verifierforge-c1-gateb-v0123` is therefore required rather
than reconstructing or editing a configuration. The existing H100 venv and
cache remain the runtime under test; only `py-spy` may be added to that venv.

## Execution and evidence

Start `c1-gateb-replay-v0123` in detached tmux with the tag worktree, the
original 0.5B config, and `VF_RUNS_DIR=/workspace/verifierforge/runs` so all
logs, metric artifacts, source-config copy, PID marker, and evidence live in
the normal persistent run directory. At T+3 minutes, install `py-spy`, select
the live `WorkerDict` process, and atomically write a full
`py-spy dump --native` (including the selected PID manifest and installer
output) under `runs/c1-gateb-replay-v0123/evidence/`. This capture is required
even if metrics have already appeared.

The total C1 timebox is 15 minutes. A JSONL metric row before expiry classifies
the H100/runtime as capable of the old workload and selects C2; no C2 repair
is authorized by this document. No metric by expiry selects C3 as an
environment-level regression candidate; it also makes no repair choice. In
either case W1 cleanup must remove tmux/processes and leave 0 MiB GPU memory.

## Non-goals, contracts, and validation

Do not alter trainer logic, configurations, dependencies other than the
authorized runtime-only `py-spy`, frozen data, contracts, or M3–M6 state. This
version updates `docs/p0-run-sheet.md`, `docs/model-trainer/`, and
`docs/infrastructure/`; contract impact is none. Validate the tag/config/data
hashes before launch, durable tmux/PGID evidence, the native stack artifact,
the metric/no-metric branch decision, and W1's independent GPU-zero check.

## C1 result

The replay used the detached tag worktree, exact source config/data digests,
and the accepted H100 runtime: Python 3.12.3, torch `2.8.0+cu128`, vLLM
`0.10.2`, verl `0.8.0`, and Ray `2.56.0`. It reached a V1 vLLM engine and
published eight bridged metric records; the first is timestamped
`2026-07-16T22:11:29.560876Z`. The synchronized metric and log digests are,
respectively,
`f6af42947100e8e938375a3f97c062cedca7a6c5c952851ab39b6932b4b297ea` and
`d965910dc18f4bcd1a1f3c062808463e468215105a3f7821ff1a45d94b573768`.

W1 teardown removed tmux and the job process group and independently verified
0 MiB GPU use. C2 is selected by the agreed metric rule. This is not a
root-cause proof: the 0.5B control first emitted at about 13 minutes, while
N6 was stopped at about 11 minutes.

`pip install py-spy` found `py-spy 0.4.2`; driver and WorkerDict native-dump
artifacts were saved, but both contain Docker's `Permission Denied`/missing
`SYS_PTRACE` error rather than a stack. A fresh pod launched with `SYS_PTRACE`
is required for that part of the diagnostic; no such infrastructure change is
authorized here.
