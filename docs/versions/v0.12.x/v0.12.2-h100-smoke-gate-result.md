# v0.12.2 — H100 smoke gate result

**Status:** completed documentation record; no implementation or retry
**Owner / worktree:** Codex / repository-root `main`

## Scope

Record the N6 H100 smoke outcome without changing trainer behavior. The run
used `grpo_v1_1p5b_h100_smoke` with one GPU, TP 1, rollout memory `0.45`,
SDPA, 30 steps, and `k=8` against the frozen runtime fixture.

**Affected areas:** no source code or contracts; this record updates
`docs/p0-run-sheet.md`, this version document, and the matching
`docs/model-trainer/` and `docs/infrastructure/` records. **Contract impact:**
none; the mock, storage, data freeze, and public contracts are unchanged.

## Evidence and decision

Dataset preparation and a FSDP `WorkerDict` started, but no `metrics.jsonl`
row appeared in 11 minutes. GPU memory remained at 7,188 MiB with 0%
utilization; Ray reserved 0.3333 GPU and reported no pending demand. The log
contains neither a completed step nor an error/traceback, so the evidence does
not identify a root cause or support a repair choice.

`vf kill n6-h100-smoke` completed its W1 cleanup and a follow-up GPU check was
0 MiB. The raw log is retained on the persistent executor volume at
`/workspace/verifierforge/runs/n6-h100-smoke/train.log`.

## Validation

The record is based on the detached job's command/log, a final `nvidia-smi`,
the missing metric artifact, Ray resource status, and the W1 post-kill tmux,
process, and GPU-zero checks. This documentation-only commit requires a clean
diff check; it does not claim a new test or training result.

## Non-goals and stop condition

This version makes no trainer, runtime, model, or configuration change. N7
and all M3–M6 jobs remain blocked. A future retry must first document a
specific diagnostic/repair plan and validation criteria in this version area,
the model-trainer area, and the infrastructure area.
