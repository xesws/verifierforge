# v0.28.3 — serving smoke beside a paused trainer

Checkpoint publication still requires conversion, `/v1/models`, and one real
completion. On the L4, the trainer remains resident while the bridge performs
that gate. A 0.10 vLLM ceiling is lower than the resident trainer allocation,
so it produces negative KV-cache capacity even though the converted 0.5B model
itself loads in under 1 GiB.

The smoke ceiling becomes a validated 0.90. This is an upper allocation target,
not eager reservation: model length remains 64, max sequences remains one,
eager mode remains enabled, and vLLM exits before training continues. Failure
evidence retains enough of the engine log to include the first root cause.
No GRPO configuration, data, reward or checkpoint cadence changes.
