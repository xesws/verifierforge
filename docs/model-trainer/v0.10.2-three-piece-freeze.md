# v0.10.2 — exact training-pool binding for Gate B

The existing loader still reads `trainer/data/nl2sql_v1.jsonl`; U3 publishes a
byte-identical alias of the frozen 50-row training pool at that path. This is a
data binding, not a trainer logic or configuration change. The loader's stable
40/10 split therefore consumes only frozen training-pool records.

After the tag is pushed and evaluator GPU memory is released, the existing
`grpo_v1_0p5b` detached job may run. Its metrics are training monitoring only;
all improvement claims remain held-out-only.
