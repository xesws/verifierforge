# v0.10.1 — Gate B remains downstream of U2/U3

The existing 0.5B detached Gate B may start only after the repaired U2 training
pool run passes all three predicates and U3 freezes the training pool, held-out
set, and verifier v2 provenance. It consumes the 50-row training pool only.

The held-out baseline captured in this recovery is the sole pre-training number
for later before/after and DoD claims. No trainer code, GRPO configuration, or
data-path configuration changes are part of v0.10.1.

**Status:** the U2 training-pool gate now passes; Gate B is still blocked until
the separate U3 freeze binds the three required identities.
