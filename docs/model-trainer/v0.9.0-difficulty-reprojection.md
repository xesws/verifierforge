# v0.9.0 — projected-data handoff boundary

The B2 output is a separate, ignored 50-row candidate artifact; it does not
replace `trainer/data/nl2sql_v1.jsonl` in v0.9.0. It retains the trainer's
required `id`, `question`, `reference_sql`, and `expected_results` fields and
adds selection provenance ignored by the current loader. Only O5, after B3
passes, may atomically promote that path into the trainer fixture as part of a
freeze commit. No GRPO or trainer configuration work belongs to this version.

The completed B2 artifact was loaded through the unchanged loader and produced
the expected 50 records and deterministic 40/10 split. It remains a separate
dataset path until a passing B3 Gate A authorizes freeze.
