# v0.3.0 — Model Trainer: RunPod Runtime Provisioning

## Planned Change

Stage the smallest two Qwen instruct models for later trainer smoke work and make a best-effort attempt to stage the larger GPT OSS model. Add `verl` and `vllm` to the trainer-only dependency set, then attempt a single timeboxed installation in the RunPod virtual environment.

## Model Download Manifest

| Model | Required outcome | Destination |
| --- | --- | --- |
| `Qwen/Qwen2.5-0.5B-Instruct` | Download requested | `/workspace/hf-cache` |
| `Qwen/Qwen2.5-1.5B-Instruct` | Download requested | `/workspace/hf-cache` |
| `openai/gpt-oss-20b` | Best effort; failure is non-blocking | `/workspace/hf-cache` |

Record resolved revisions and cache snapshot paths after each completed download. Do not copy weights into the repository, `runs/`, or the laptop.

## Dependency Compatibility / Resolution Record

Before installation, capture Python, CUDA driver/runtime, Torch (if present), and pip metadata. The preflight found a CUDA 12.8 L4 worker and no Torch in the project venv. The initial `vllm==0.25.1` selection installed CUDA 13 dependencies and failed import; its version is also outside verl 0.8.0's declared `vllm` extra range (`>=0.8.5, <=0.12.0`). Preserve that evidence and use one clean replacement venv with `vllm==0.10.2` (the CUDA 12.8-compatible documented line) and `verl[vllm]` at commit `cb367c201187259e109a90f58af9ad9659126429`. This installs the base verl dependencies, including Ray, rather than intentionally suppressing them. Record final resolved versions and an import check if successful, or the first decisive incompatibility if not.

## Explicit Non-training Boundary

This version does not run GRPO, create a verl config, connect a verifier to a reward adapter, start vLLM serving, alter checkpoint logic, or modify any contract. It provisions prerequisites only.

## Acceptance Evidence

Record tmux logs, command exit status, elapsed time, model cache locations, and `python -c` import/version results where applicable. If the 45-minute limit expires or installation fails, record it in `NOTES.md` and leave training for a later, separately documented version.
