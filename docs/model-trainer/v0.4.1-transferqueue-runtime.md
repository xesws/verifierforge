# v0.4.1 — Model Trainer: TransferQueue Runtime

verl 0.8 initializes its TransferQueue integration on the D2 GRPO path. The
first real preflight exposed the missing package before model loading, so the
trainer requirements pin `TransferQueue==0.1.7` exactly as requested by the
runtime. The retry keeps the same model, data, overrides, and two-step target;
it is evidence of a dependency correction rather than a new training result.
