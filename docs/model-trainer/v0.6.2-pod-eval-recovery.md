# v0.6.2 — Gate B after local evaluation freeze

**Status:** in progress. Linked plan: `docs/versions/v0.6.x/v0.6.2-pod-eval-recovery.md`.

No trainer behavior changes in this version. After the v0.6.2 freeze tag binds
the datasets, verifier, and local eval evidence, the existing
`grpo_v1_0p5b` command runs as `p0-gateb-v062` through detached `vf train`.
The vLLM evaluation server must be stopped first so the L4 is free.

The final pass@1 sequence, curve, artifacts, and a strictly smoke-only
conclusion are recorded in the remote log and run-sheet. Failure retains
checkpoints/logs without automatic tuning or retry.
